'use strict';

/**
 * PUBLIC_INTERFACE
 * @typedef {Object} Note
 * @property {string} id - Unique identifier (UUID v4)
 * @property {string} title - Title of the note (non-empty string)
 * @property {string} content - Content of the note (string, can be empty)
 * @property {string} createdAt - ISO timestamp when the note was created
 * @property {string} updatedAt - ISO timestamp when the note was last updated
 */

module.exports = {};
